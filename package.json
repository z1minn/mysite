{
  "name": "mysite",
  "version": "1.0.0",
  "scripts": {
    "build:css": "tailwindcss -i src/styles.css -o public/output.css --minify",
    "dev": "live-server public --port=3000"
  },
  "devDependencies": {
    "tailwindcss": "^3.4.0",
    "postcss": "^8.4.0",
    "autoprefixer": "^10.4.0",
    "live-server": "^1.2.2"
  }
}
